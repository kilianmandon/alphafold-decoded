name: alphafold 
channels:
  - nvidia
  - pytorch  # Add the PyTorch channel
  - defaults
dependencies:
  - python=3.9
  - pytorch
  - torchvision
  - matplotlib
  - pytorch-cuda=11.8
  - ipykernel
  - ipywidgets=7.7.1
  - ipympl
  - pip 

